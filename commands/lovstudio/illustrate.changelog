# Changelog for illustrate

## v2.4.0 - 2026-04-05
- **Step 7 精确插入定位规则**：新增4条硬性定位规则，解决组装阶段图片位置不精确问题
  - **先提后图**：图片必须在所配内容首次被提及之后，禁止"图先于文"
  - **不割裂语义单元**：连续引用块、递进收束段落对、论点论据、排比并列结构视为不可分割
  - **收束优先**：概念有金句/总结时，图插在收束句之后而非之前
  - **朗读自检**：插入后朗读前后文，感觉"话说到一半被打断"则位置错误
- **触发原因**：实际使用中出现3类定位错误——清华报告图出现在报告被提及之前、Altman/Amodei合影割裂两人引言、WYFIWYG图打断"会说即会做"收束。根因是Step 3只规划章节级位置，Step 7缺少段落级精确定位规则

## v2.3.0 - 2026-04-03
- **实体提取步骤（Step 2 新增）**：规划插图前必须先扫描文档提取所有可联网检索的真实实体（产品/人物/论文/开源项目/博文等），输出实体表作为插图方案的输入
- **自检门禁**：方案完成后强制统计 AI 生图比例，超过 30% 必须逐张审查是否能改为联网检索
- **实体表驱动**：每个提取的实体必须对应至少一张联网检索图，从"原则声明"升级为"流程约束"
- **方案表增加列**：新增"关联实体"和"搜索策略/prompt"列，强制显式标注每张图的来源依据
- **搜不到才降级**：联网检索失败后需记录失败原因，再降级为 AI 生图
- **触发原因**：实际使用中尽管已有"联网检索>>AI生图"原则，仍默认全部 AI 生图，被用户纠正。原则层面的声明不够，需要流程层面的硬约束

## v2.2.0 - 2026-03-30
- **插图密度提升**：从每800-1200字一张改为每400-600字一张，每个章节至少1-2张图
- **AI生图占比硬限制**：AI生图不超过总数的20%，其余全部联网检索真实素材
- **真实素材覆盖扩展**：新增人物必配真实照片、事件必配新闻图片、地点必配实景照片规则
- **类型决策树扩展**：新增"地点"类别，覆盖产品/人物/事件/地点四类真实素材

## v2.1.0 - 2026-03-30
- **Hero Image**：新增开篇英雄图规则，文章第一张图必须是高传播度的叙事总结图（提炼核心演进弧线为3-5个里程碑）
- **产品必配真实截图**：文中提到的具体产品/工具，必须联网检索真实产品界面截图，不允许用AI生图替代
- **图片质量校验**（Step 6.5）：生成/下载后用 Read 工具验证图片内容是否匹配描述，不合格则重新搜索/生成

## v2.0.0 - 2026-03-21
- **类型决策树**：新增三种类型（联网检索/AI生图/数据图表），联网检索优先于AI生图
- **数据图表流程**：涉及可量化数据时，先调研获取可溯源数据点，确认后再生成图表
- **品牌风格自动加载**：自动读取 design-guide.md，注入品牌色彩和质感到所有AI生图prompt
- **插图位置新规则**：新增「数据横比」和「演变过程」原则，多产品并列配对比图而非逐个配图
- **图片命名语义化**：`ill-<n>-<slug>.png` 替代纯数字命名
- **数据图表标注来源**：图表底部必须标注 Sources，alt文本含关键数据点

## v1.0.0 - 2026-03-21
- 初始版本
- 智能分析文档结构，确定插图位置
- 支持 AI 生图（via gen_image.py）和联网检索两种模式
- 插图方案用户确认流程（可 --auto 跳过）
- 并行生成图片
- 输出 -illustrated.md 增强版文档

Author: 公众号：手工川
