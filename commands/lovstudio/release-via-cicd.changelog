# Changelog for release-via-cicd

## v8.4.0 - 2026-04-06
- 首次发布默认 v0.1.0（不是 v1.0.0），1.0+ 需用户明确指定
- 自动版本推断增加 0.x 保持逻辑：patch → 0.x.y+1, minor → 0.x+1.0
- Shell 项目默认版本从 v1.0.0 改为 v0.1.0
- 添加版本号哲学：0.x = 持续演进，1.0 = 稳定 API 承诺
- 已有错误 1.0+ tag 时应提议重写

## v8.3.0 - 2026-03-14
- 新增 Vercel 自动部署检测：push 后检测 .vercel/ 目录，等待 Git Integration 触发，未触发则 fallback 到 `vercel --prod`
- allowed-tools 新增 `Bash(vercel:*)`
- 陷阱表新增 Vercel 未自动部署处理方案

## v8.2.0 - 2026-02-28
- 所有项目类型强制使用 CHANGELOG.md 作为 release notes 来源
- 禁止使用 `generate_release_notes: true`
- Shell 项目 workflow 模板添加 CHANGELOG.md 提取步骤
- 纯 Node 本地发布路径改用 CHANGELOG.md 提取
- Setup 检查清单新增 CHANGELOG.md 检查项
- Publish 前置检查新增 CHANGELOG.md 存在性检查
- 陷阱表新增 2 条：release notes 空白问题、CHANGELOG.md 不存在处理

## v8.1.0 - 2026-01-31
- 新增 Step 0: 开场笑话，在发布前讲一个程序员笑话放松心情
- 包含 7 个精选笑话，每次随机选一个

## v8.0.1 - 2026-01-14
- 默认使用 patch 版本，更保守安全
- 只有用户显式指定 minor/major 或检测到 BREAKING 变更时才使用其他版本
- BREAKING 变更时会询问用户确认是否使用 major

## v8.0.0 - (previous)
- 根据 commit 类型自动推断版本（fix→patch, feat→minor, BREAKING→major）

Author: 公众号：手工川
