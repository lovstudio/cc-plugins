# Changelog for install-tauri-logo

## v4.1.0 - 2026-04-21
### Fixes (learned from real usage in lovclip)
- **Tray icon 白色 template 在 light mode 不可见**: 改用 alpha 作为形状，RGB 填纯黑。macOS `icon_as_template(true)` 会自动反色。避免 "-colorize white" 在透明底 logo 上失效的陷阱
- **Silent fallback 隐藏问题**: Rust 代码模板从 `.unwrap_or_else(|_| default_window_icon)` 改为 `.expect()`，解码失败显式 panic
- **新增诊断日志**: Rust 模板加 `log::info!` 打印 embedded bytes 和 decoded 尺寸，方便排查
### Features
- **Step 1 新增 source 验证**: 打印 path + md5 + dimensions，提醒用户确认是否 /gen-logo 已 publish
- **Step 2 新增 theme color 自动检测**: 从 `CLAUDE.md` 读取，支持 `--theme-color` 覆盖。避免主题色硬编码漂移（v3→v4 曾从 #D97757 变到 #E76F4D，现统一用项目源）
- **Step 7 强化重建提醒**: `include_bytes!` 是编译期嵌入，明确告知必须重启 `pnpm tauri:dev`，附带 log 验证项
### Documentation
- 新增 "Common failure modes" 小节（白底源 logo / 白色填充陷阱）
- 补充 Design Principles（fail loud, project-aligned, template correctness, compile-time awareness）

## v4.0.0 - 2025-12-25
### Breaking Changes
- **移除 Step 3 (Sync to public/)**: Web 相关的 logo 配置已拆分到 `/install-web-logo`
- 步骤重新编号：Step 4-7 → Step 3-6
- **新增 Step 7**: 完成后询问用户是否继续执行 `/install-web-logo`

## v3.1.0 - 2025-12-25
- **macOS 图标规范**: 824x824 圆角矩形在 1024x1024 画布内，与系统 app 大小一致
- **Logo 比例**: 60% → 65%，视觉更饱满
- **主题色修正**: #E66F4C → #E76F4D

## v3.0.1 - 2025-12-25
- 修正主题色：#D97757 → #E66F4C（与设计系统保持一致）

## v3.0.0 - 2025-12-25
### Breaking Changes
- **Dock Icon 重新设计**: 圆角矩形背景（陶土色 #D97757）+ 白色 logo
  - 参考 macOS Big Sur+ 系统 app 风格
  - 圆角比例 ~22%，logo 缩放 60%
- **Tray Icon 规格更新**:
  - 改为白色 template icon（原为黑色）
  - 38x44 内容 + 56x44 画布，横向间距与其他 app 对齐

## v2.1.0 - 2025-12-25
- Add Step 3: Sync logo to public/ directory for frontend usage
- Fix: Frontend `<img src="/logo.png">` references now get updated automatically

## v2.0.0 - 2025-12-25
- Initial versioned release
- Use official `npx tauri icon` for icon generation
- Support tray icon generation with ImageMagick
- Optional Rust code modification for custom tray icons

Author: 公众号：手工川
